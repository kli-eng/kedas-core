# ============================================================
# KEDAS Core — Dependencias Python
# Archivo: requirements.txt
#
# REDUCIDO 29-jul-2026 (DEC-59) respecto a la copia heredada de Premium.
# El requirements.txt anterior traía TODO el stack de Premium (torch,
# transformers, spacy, scikit-learn, shap, lime, fairlearn, mlflow,
# weasyprint, plotly, dash, matplotlib...) -- varios GB de dependencias
# que ningún archivo de kedas-core (auth.py, estudiantes.py, dashboard.py,
# kolibri.py, admin.py, rea.py) usa. Se deja solo lo que Core necesita.
# ============================================================

# ── API REST ──────────────────────────────────────────────────
fastapi==0.115.0          # MIT
uvicorn[standard]==0.30.6 # MIT
python-multipart==0.0.9   # Apache 2.0
pydantic==2.7.4           # MIT
email-validator==2.1.0    # MIT — validación emails FastAPI

# ── Autenticación (auth.py) ───────────────────────────────────
python-jose[cryptography]==3.3.0  # MIT
passlib[bcrypt]==1.7.4    # BSD
bcrypt==3.2.2              # Apache 2.0 — pin <4.0.0 requerido por passlib
slowapi==0.1.9              # rate limiting en login
pyotp==2.9.0                 # MFA/TOTP (migración 015, transversal)
qrcode[pil]==7.4.2           # generación de QR para MFA

# ── Base de datos ─────────────────────────────────────────────
asyncpg==0.29.0           # Apache 2.0 — async PostgreSQL

# ── Utilidades ────────────────────────────────────────────────
python-dotenv==1.0.1      # BSD

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.2             # MIT
pytest-asyncio==0.23.7    # Apache 2.0
httpx==0.27.0             # BSD-3-Clause — cliente HTTP async para tests
