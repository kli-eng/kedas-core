# ============================================================
# KEDAS v3.0 — Dependencias Python
# Archivo: requirements.txt
# Instalar: pip install -r requirements.txt
# Licencias: ver Parte II §2.1 del Documento Técnico Maestro
# ============================================================

# ── API REST ──────────────────────────────────────────────────
fastapi==0.115.0          # MIT
uvicorn[standard]==0.30.6 # MIT
python-multipart==0.0.9   # Apache 2.0
pydantic==2.7.4           # MIT
python-jose[cryptography]==3.3.0  # MIT
passlib[bcrypt]==1.7.4    # BSD

# ── Base de datos ─────────────────────────────────────────────
asyncpg==0.29.0           # Apache 2.0   — async PostgreSQL (API FastAPI)
psycopg2-binary==2.9.9    # LGPL          — sync PostgreSQL (ML pipeline)
sqlalchemy==2.0.31        # MIT

# ── Machine Learning ──────────────────────────────────────────
scikit-learn==1.5.2       # BSD-3-Clause
numpy==1.26.4             # BSD
pandas==2.2.2             # BSD-3-Clause
scipy==1.13.1             # BSD-3-Clause
joblib==1.4.2             # BSD-3-Clause

# ── Explicabilidad XAI ────────────────────────────────────────
shap==0.46.0              # MIT
lime==0.2.0.1             # BSD

# ── Equidad algorítmica ───────────────────────────────────────
fairlearn==0.10.0         # MIT

# ── MLflow — tracking y auditoría ─────────────────────────────
mlflow==2.14.1            # Apache 2.0

# ── NLP (evaluación de ensayos) ───────────────────────────────
spacy==3.7.5              # MIT
# Modelo español: python -m spacy download es_core_news_lg
transformers==4.42.4      # Apache 2.0
torch==2.3.1              # BSD — requerido por Transformers

# ── Reportes PDF ──────────────────────────────────────────────
weasyprint==69.0
jinja2==3.1.4             # BSD-3-Clause

# ── Visualización (dashboards, KPIs) ─────────────────────────
plotly==5.22.0            # MIT
dash==2.17.1              # MIT
matplotlib==3.9.1         # PSF License
seaborn==0.13.2           # BSD-3-Clause

# ── HTTP ─────────────────────────────────────────────────────
requests==2.32.3          # Apache 2.0
httpx==0.27.0             # BSD-3-Clause  — async HTTP client

# ── Utilidades ────────────────────────────────────────────────
python-dotenv==1.0.1      # BSD
click==8.1.7              # BSD-3-Clause

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.2             # MIT
pytest-asyncio==0.23.7    # Apache 2.0
# httpx ya declarado arriba — cliente HTTP async para tests de API

# — Pins transitivos (antes en command: override del compose) ——————————————
email-validator==2.1.0      # MIT  — validación emails FastAPI
bcrypt==3.2.2               # Apache 2.0 — pin <4.0.0 requerido por passlib
pypdf==6.13.3               # MIT — extracción texto PDF para verificación RICE
slowapi
pyotp
qrcode[pil]
